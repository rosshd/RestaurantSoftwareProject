public class Manager extends Worker {
    public Manager(int workerId, String name, String contactInfo, String availability) {
        super(workerId, name, contactInfo, "Manager", availability);
    }
}
